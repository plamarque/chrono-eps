# Mots de passe Bubblewrap (build Android AAB)
# Copiez ce fichier en .env et remplissez les valeurs.
# .env n'est pas commité (voir .gitignore).

BUBBLEWRAP_KEYSTORE_PASSWORD=
BUBBLEWRAP_KEY_PASSWORD=
